# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (managed workflow — regenerated by prebuild/EAS; never hand-edited)
ios/
android/

# Environment & secrets — never commit real keys (.env.example is the template)
.env
.env.*
!.env.example

# EAS / builds
*.ipa
*.apk
*.aab

# macOS
.DS_Store

# Testing & tooling caches
coverage/
.eslintcache
*.tsbuildinfo

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

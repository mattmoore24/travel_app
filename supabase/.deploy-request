# Touch this file (any change) to trigger .github/workflows/supabase-deploy.yml
# on the current branch: migrations, Edge Functions, and function secrets.
# The line below is just a log of deploy requests.

2026-08-17 — Phase 0-5 schema, push-worker + moderation-worker (initial provisioning)
2026-08-17 — deploy run 2: secrets configured, provisioning the project
2026-08-17 — deploy run 3: Phase 6 launch hardening (rate limits, admin views, delete-account)
2026-08-17 — deploy run 4: Phase 7 schema (pending_moderation enum, photo slots, guest mode,
             establishment rooms + reactions) and ANTHROPIC_API_KEY sync for moderation-worker
2026-08-17 — deploy run 5: pg_cron schedules for moderation-worker and push-worker
             (credentials read from Vault, never committed)
2026-08-17 — deploy run 6: worker diagnostics (invoke log + worker_status)
2026-08-18 — deploy run 7: set_worker_credentials (swap-proof vault setter)
2026-08-18 — deploy run 8: seed_launch_pins() and seed_launch_establishments()
2026-08-19 — deploy run 9: profile-first (occupation, upcoming-trip visibility, all overlaps over 180 days)
2026-08-19 — deploy run 10: profile-first fixes (no bulk trip reads, deduped matches, aligned horizon)
2026-08-19 — deploy run 11: pin note + place label
2026-08-19 — deploy run 12: copy pass on push notifications
2026-08-21 — deploy: support delivery (push channel, submit/status RPCs)
2026-08-21 — deploy: sync RESEND_API_KEY + SUPPORT_INBOX to the support mailer
2026-08-21 — deploy: re-sync RESEND_API_KEY after the founder corrected it
2026-08-21 — deploy: support retry window (next_attempt_at, backoff, higher ceiling)
2026-08-21 — deploy: SUPPORT_INBOX corrected to the Resend account address
2026-08-22 — deploy: unread state (chat_prefs.last_read_at, mark_chat_read, my_chats.unread_count)
2026-08-22 — deploy: my_chats.first_message_element (the anchor an accepted chat opened on)
2026-08-22 — deploy: featured traveler needs a face, daily first-message cap, reword preview
2026-08-22 — deploy: travel prompts, daily mutual spotlight
2026-08-22 — deploy: room_info (the name a non-member sees), pinned messages
2026-08-22 — deploy: review fixes (spotlight restates the policy filters and takes a day lock, first-message cap serialised, pin slots freed with their message, pin refusals stop being an existence oracle)
2026-08-23 — deploy: keep the map alive (daily curated-pin refresh, heat cells grouped by cell)
2026-08-23 — deploy: curated pins stay current (stale seeds cleared, city_pins grants restated)
2026-08-23 — deploy: who can see you (profile_audience, discovery_pair_ok, gated on the badge)
2026-08-23 — deploy: verified_nonbinary audience (enum rebuilt, predicates restated)
2026-08-23 — deploy: an invite link opens for the person it was sent to (anon preview grant)
2026-08-23 — deploy: guests can chat (is_guest, the refusals, the ceilings, the janitor)
2026-08-23 — deploy: retry guests-can-chat, without touching auth.users
2026-08-23 — deploy: venue rooms stay read-only for a guest, in the database and not just the client
2026-08-23 — deploy: the audience setting reaches the signed-out map too (public_city_pins)
2026-08-26 — deploy: row-level security on the five public tables the Supabase linter flagged
2026-08-27 — deploy: top priorities on the profile (profile_priorities, screened like the bio)
2026-08-27 — deploy: Places, phases 13-18. The establishments rename with all eight
             dependent functions recreated; §7 rule 8 as six triggers; photos, links,
             hours and posts behind one visibility predicate; the six-digit listing
             code and the storefront badge; reports that email SUPPORT_INBOX and queue
             an impersonation scan on the FIRST report; messages that go straight
             through on kind='business'; Beli-style ratings.
             After this run, in the SQL editor:
               select public.seed_launch_businesses();
               select public.seed_launch_business_content();
             And add the `storefront` and `impersonation` keys to the
             MODERATION_PROMPTS secret (prompts.example.json documents them) — until
             then those two queues pause and everything else keeps running.
2026-08-27 — deploy: sync MODERATION_PROMPTS_BUSINESS to the moderation-worker. The
             founder added the secret; the storefront and impersonation queues stay
             paused until the function actually holds it. No migration in this run.
2026-08-27 — deploy: delete-account now takes a place's listing with the account.
             businesses.owner_user_id is ON DELETE SET NULL, so the listing used to
             outlive the account that made it. No migration; the Edge Function alone.
2026-08-27 — deploy: 20260827160000_places_polish.sql, everything the final audit
             found on the database side. business_detail gains `claimed` (DROP first,
             grants restated); an uncertain storefront check can be finished by a
             person; register_business sets display_name and a rename carries it;
             the owner is a moderator in their own room; a business cannot report a
             rival; is_business_account is revoked from clients; website_url goes
             through the link validator; a re-used confirmation code still relists;
             writing to a place you have written to before actually sends.
2026-08-27 — deploy: "Chat is active until". max_stay_until becomes nullable (no end
             date); group_closes_at() at noon UTC the day after; can_send_in_chat
             enforces it; the sweep and the archiver leave a closed group alone;
             create_group/update_group take and clear the date (update_group DROPPED
             and recreated with p_clear_max_stay — a defaulted parameter is an
             OVERLOAD, not a replacement); join clamps instead of refusing;
             group_invite_preview DROPPED and recreated with `closed` so an ended
             chat is not described as a withdrawn link. The created_at-anchored
             groups_max_stay_sane constraint is gone — it refused every future date
             on a group older than 400 days, which is the one control that reopens
             a closed chat. Past-dated groups are pushed to +30 days so nothing goes
             dark on deploy day.
2026-08-28 — deploy: 20260828120000_closed_groups_stay_readable.sql. Keeping a closed
             group's room_members row was only half the promise — every gate reads
             `expires_at > now()` and none of them asks whether a row exists, so a
             lapsed seat lost the group about a week after it closed and the admin's
             own thread opened empty. is_room_member and my_chats (restated whole,
             OUT columns untouched, so create-or-replace and the grants survive) both
             recognise a closed group now. Nothing here lets anybody post.
2026-08-28 — deploy: 20260828140000_room_unsend_and_mute.sql and
             20260828150000_pin_lifetime_is_not_the_clients.sql.
             room_messages DROPPED and recreated with unsent_at (grant
             restated) — it predated unsend, so a withdrawn message came back
             from a group with removed=false and no flag and drew an empty
             bubble for everyone. enqueue_message_push gains the mute
             predicate its direct arm never had and reads chat_prefs on both
             arms, because room_members.muted is a column authenticated
             cannot write — muting struck the bell through and kept ringing
             the phone. my_chats takes the opening request through a lateral,
             so two hellos in one chat stop returning the conversation twice.
             And pins loses its blanket INSERT for a per-column grant: the 72h
             CHECK is anchored to created_at, which a client could set.
2026-08-28 — deploy: 20260828160000_businesses_not_places.sql. Five Postgres
             exception strings say "place" and reach the screen verbatim
             (saveFailureMessage returns error.message unchanged, by design),
             so an alert said "nobody runs this place yet" while the app said
             business. report_business, message_business and rate_business
             restated whole with the new words; no OUT columns change, so
             create-or-replace and the grants survive, restated anyway.
2026-08-28 — deploy: 20260828170000_moderation_answers_now.sql. A photo posted
             into a chat waited on the clock, not on Claude: cron fires every
             minute (mean 30s of nothing), and chat photos drained third of
             six. The insert is the trigger now — worker_pokes throttles to one
             invocation per three seconds so a room where six people paste at
             once is still one call, poke_worker swallows its own failures
             because a photo that cannot be SENT is worse than one that waits,
             and the every-minute cron stays exactly as it was as the backstop.
             admin_moderation_latency measures queued-to-verdict per queue and
             is the only thing the app's "usually about N seconds" may quote.
             The worker also reorders its queues (chat photos first, held
             hellos second) and runs those two at low effort — the two calls
             somebody watches a placeholder for. Verification, storefronts and
             impersonation stay careful: nobody is waiting on those, and being
             wrong about who somebody IS costs more than a few seconds.
2026-08-28 — deploy: 20260828180000_a_photo_says_it_is_being_checked.sql.
             room_messages DROPPED and recreated with photo_state (grant
             restated): masking image_path until a verdict lands was ALL it
             did, so a photo in review came back as a row with no image and
             usually no body, and the thread drew an empty coloured bubble for
             the whole wait — the founder's "tiny bubble". The state is not a
             secret (every photo here is checked) and the PATH stays masked,
             except for the sender, who could already read their own upload
             through chat_photos_select_own and was being shown a blank tile
             about their own picture.
2026-08-28 — deploy: 20260828190000_a_poke_never_waits_for_a_lock.sql.
             poke_worker claimed its throttle slot with `on conflict do
             update`, which takes a row lock — so two people posting a photo in
             the same second serialised on one row, on the path of SENDING A
             MESSAGE. Short in practice, and the failure mode is bad in a
             specific way: a wait long enough to hit statement_timeout raises
             query_canceled, which plpgsql's `when others` does not catch, so
             the handler that exists to keep a poke harmless would let it
             through and the insert would fail. A try-advisory lock removes the
             wait entirely; losing that race is the same answer the throttle
             gives anyway. create-or-replace — the signature is unchanged and
             four triggers depend on the caller.
2026-08-29 — deploy: 20260829120000_a_pin_anyone_can_join.sql and
             20260829130000_add_people_without_a_link.sql.
             A pin can now carry a group chat: groups.pin_id, pointing from
             the group AT the pin and not the other way round, because pins are
             hard deleted at 72 hours and a chat_id on pins would take the
             conversation with it (ON DELETE SET NULL, so the chat lives on as
             an ordinary no-end-date group). post_joinable_pin does the pin and
             the group in one transaction — every BEFORE INSERT trigger on pins
             still fires inside it, so the geofence, the 10-live cap, the 30/24h
             churn cap and the guest and business refusals are unchanged.
             join_pin_chat needs no token: it checks the pin is live and that
             the OWNER is discoverable to you, which is the founder's rule that
             the audience filter follows the poster and not the joiners.
             city_pins and public_city_pins DROPPED and recreated with chat_id
             and crew (grants restated on both — public_city_pins keeps anon).
             The second migration writes down "people you already know" —
             an active direct chat or an active traveler group, never a venue's
             open room, never a guest — and hangs three doors off it:
             people_you_know, add_to_group (any member, not only the admin) and
             open_direct_chat (no request, no accept). Two §7 rules needed care.
             Handles: chats gains opened_from_room, and the social_handles
             policy moves to handles_unlocked_for, which for a room-opened chat
             requires BOTH people to have spoken — stricter than the single
             accept tap it stands in for, and no existing chat is affected.
             has_accepted_chat is deliberately untouched: its six other callers
             ask "do these two have a conversation", and the answer is yes.
             First messages: open_direct_chat screens with screen_first_message
             and a blocked one creates nothing at all, the same shape
             message_business already uses.
             707 existing pgTAP assertions still pass; 50 new ones in
             26_joinable_pins_and_knowing_people.test.sql cover the above.
2026-08-29 — deploy: 20260829140000_a_guest_map_stays_faceless.sql. Self-review
             of yesterday's own migration: public_city_pins strips every trace
             of a person from a guest's map — no user_id, no name, no age, no
             photo — and pin_crew walked straight past that, because it answers
             for anybody holding the `authenticated` role and an anonymous
             guest holds it. That made the faces on an open plan a free read of
             names and photo paths for every open pin in a city, without
             joining anything, which is exactly the harvest the guest feed
             exists to prevent. It returns nothing for a guest now. A guest can
             still JOIN an open plan and see the room from the inside, the same
             as a group they hold a link for: that is a membership row somebody
             can see and an admin can remove, and it is capped at ten. Same OUT
             columns, so create-or-replace; grant restated anyway.
2026-08-29 — deploy: 20260829150000_a_code_that_never_arrives_says_so.sql.
             The founder was told a business code had been sent and never
             received one; a second address on another provider worked. That is
             Resend's sandbox rule, which this project has been bitten by once
             already (the backoff comment in support-mailer records it): with no
             verified sending domain, onboarding@resend.dev may only deliver to
             the Resend account's own address. The mailer recorded the refusal
             in outbound_mail.delivery_error faithfully and NOTHING HAS EVER
             READ IT, so the screen went on saying "we sent a code" about mail
             that was never going anywhere. my_business_code_status() is that
             read: scoped to the caller's own business, to the address
             currently on file, to the single newest code, and it never returns
             the provider's error text. The screen now says "That address
             bounced" and leads with "Use a different address".
             Also: poke_worker's allowlist gains 'support-mailer'.
             invoke_edge_worker already had it; only poke_worker's mirror of
             that list was missing it, so a code sat waiting up to five minutes
             for a cron tick out of the twenty minutes it is alive for.
             FOUNDER ACTION, and the real fix: verify a domain in Resend and
             set SUPPORT_FROM to an address on it. Until that is done no
             business but the founder's own can receive a code at all.
2026-08-29 — deploy: 20260829160000_a_business_says_where_it_is.sql.
             businesses gains `address` (<=160, screened, in the public select
             grant and the owner's update grant) so the founder's "enter an
             address and confirm the pin location" has somewhere to live that
             is NOT place_label — place_label is the finding-the-door note
             ("blue door") that travelers already read under Getting there, and
             an address written into it would delete the more useful of the
             two. Two columns is also the only way "keep the address I typed
             while I move the marker" can be true.
             AND THE GEOFENCE, which nobody asked for and which matters more:
             there wasn't one. haversine_km had exactly one caller in the whole
             schema (validate_pin), register_business validated the caller and
             nothing about geography, and businesses.city_id references cities
             rather than launch_cities — so a marker could sit anywhere on
             earth while the listing claimed Bangkok, and business-signup's own
             comment said the server refused exactly that. register_business
             DROPped and recreated (a defaulted parameter is an OVERLOAD, not a
             replacement) with the address and the radius check; grants
             restated. update_business_location is the only way to move a
             marker afterwards — lat/lng are withheld from the client grant on
             purpose — and it re-runs the geofence and leaves the typed address
             alone. business_detail DROPped and recreated with `address`.
             771 pgTAP assertions, 7 new and about exactly this.
2026-08-29T17:37:42Z A business photo is ever seen
             business_photos.moderation_status defaulted to 'pending' and
             NOTHING ever moved it: profile_photos gets moderate_photo_stub on
             insert, business photos got the same column, the same enum, the
             same '= approved' filter on every read, and no trigger. So no
             photo of a business had ever been visible to a traveler, and
             business signup's photo step could not be passed at all — the
             count it gates on comes from business_detail, which filters on
             'approved', so it was pinned at zero however many went in.
             Trigger for the flag-off case, apply_business_photo_verdict plus
             a worker branch for the flag-on case (production runs flag-on, so
             the trigger alone would have swapped one invisible state for
             another). 779 pgTAP assertions, 8 new and about exactly this.

2026-08-29T21:05:00Z A business is not a traveler
             The founder tested as a business and found it could join things.
             Every one of these accepted the call: join_room (ANY listed
             business's room, including its OWN), join_pin_chat, create_group,
             post_joinable_pin, set_visibility, and a straight insert into
             pins. The client hid some of the buttons; the anon key is in the
             app bundle, so hiding a button is not a rule. assert_not_business
             is now the one place that says no, called from all six, plus a
             trigger on pins so the table itself refuses. The departure date
             the founder objected to goes with it: it is join_room's second
             argument and a business never reaches join_room.
             788 pgTAP assertions, 9 new and about exactly this.

2026-08-30 — 20260830000000_a_business_is_served_no_travelers.sql
             The other half of the same rule: a business account is now
             served no traveler identities at all. city_pins, traveler_trips,
             pin_crew and featured_traveler return zero rows to a business
             caller, and open_direct_chat refuses one outright rather than
             failing to find a shared group by accident.
             Two reads the room screen could not do are answered server-side
             instead of derived: business_for_chat now resolves a business's
             own public room (kind = 'room', not just the DM), and my_chats
             gives the person who runs a room the admin role it already
             grants them through is_room_moderator.
             804 pgTAP assertions, 16 new and about exactly this.

2026-09-01 — functions only, no migration: the moderation tick gets a wall-clock
             budget with a slice per queue, one request gets a 90s timeout and
             no SDK-level retries, and the npm imports are pinned to the
             versions the last good deploy resolved. Live backend went red on
             d0b690f with a clean first message held the full five minutes and
             the SAME commit re-run passed in eleven seconds, so this is not a
             regression being fixed — it is the mechanism that produces exactly
             that symptom being removed.
